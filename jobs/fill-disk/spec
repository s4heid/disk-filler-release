---
name: fill-disk

description: Fill the disk with random data

templates:
  run: bin/run

packages: []

properties:
  size:
    description: "The disk space to fill with random data. For example, '3MB' or '1GB', or '1GiB'."
    default: "100MiB"
  allocate_only:
    description: "If true, only allocate the disk space without writing any data."
    default: true